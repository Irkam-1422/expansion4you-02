import React from "react";
import { Contact } from "../layout/Contact";
import { ToContactButton } from "../layout/ToContactButton";

export const Footer = () => {
  return (
    <div>
      <ToContactButton />
      <Contact />
    </div>
  );
};
